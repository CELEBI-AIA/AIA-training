# Pre-commit hooks — tests run automatically before each commit.
# One-time setup: pre-commit install
repos:
  - repo: local
    hooks:
      - id: pytest
        name: pytest
        entry: python -m pytest tests/ -v --tb=short -x
        language: system
        pass_filenames: false
